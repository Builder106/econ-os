[pytest]
# Project root on sys.path so `from server.kernel import ...` works under
# bare `pytest`, not just `python3 -m pytest`.
pythonpath = .

# Default discovery path — running `pytest` with no args picks up the suite.
testpaths = simulation/tests

# Reduce surprise from the env's _calculate_utility approximation tests.
addopts = -q
